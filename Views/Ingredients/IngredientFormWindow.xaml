<Window x:Class="Project_PRN212.Views.Ingredients.IngredientFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông tin Nguyên Liệu"
        Height="320" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="txtTitle" Text="Thêm Nguyên Liệu"
                   FontSize="16" FontWeight="Bold" Foreground="#2c3e50" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Tên nguyên liệu *" Margin="0,0,0,3"/>
        <TextBox   Grid.Row="2" x:Name="txtName" Height="28" Padding="5,3" Margin="0,0,0,10"/>

        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Đơn vị *" Margin="0,0,0,3"/>
                <TextBox x:Name="txtUnit" Height="28" Padding="5,3"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Giá nhập (VNĐ)" Margin="0,0,0,3"/>
                <TextBox x:Name="txtPrice" Height="28" Padding="5,3"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="4" Text="Trạng thái" Margin="0,0,0,3"/>
        <ComboBox  Grid.Row="5" x:Name="cboStatus" Height="28" Margin="0,0,0,10">
            <ComboBoxItem Content="Available" IsSelected="True"/>
            <ComboBoxItem Content="Unavailable"/>
        </ComboBox>

        <TextBlock Grid.Row="6" x:Name="txtError" Foreground="Red" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Hủy" Width="70" Height="28" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button Content="Lưu" Width="70" Height="28" Background="#007bff" Foreground="White"
                    FontWeight="Bold" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
