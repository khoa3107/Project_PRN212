<Window x:Class="Project_PRN212.Views.Productions.ProductionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_PRN212.Views.Productions"
        mc:Ignorable="d"
        Title="Quản lý Sản Xuất" Height="500" Width="750"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Form thêm -->
        <Border Grid.Row="0" BorderBrush="#dee2e6" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="txtPlanName" Grid.Column="0" Margin="0,0,5,0"
                         Tag="Tên kế hoạch" Style="{StaticResource PlaceholderStyle}"/>
                <TextBox x:Name="txtProduct"  Grid.Column="1" Margin="0,0,5,0"
                         Tag="Tên món ăn" Style="{StaticResource PlaceholderStyle}"/>
                <TextBox x:Name="txtQty"      Grid.Column="2" Margin="0,0,5,0"
                         Tag="SL" Style="{StaticResource PlaceholderStyle}"/>
                <TextBox x:Name="txtNote"     Grid.Column="3" Margin="0,0,5,0"
                         Tag="Ghi chú" Style="{StaticResource PlaceholderStyle}"/>
                <Button  x:Name="btnAdd"      Grid.Column="4" Content="Thêm" Width="60"
                         Background="#28a745" Foreground="White" Click="BtnAdd_Click"/>
            </Grid>
        </Border>

        <!-- Thông báo lỗi -->
        <TextBlock x:Name="txtError" Grid.Row="1" Foreground="Red" Margin="0,0,0,5"/>

        <!-- Danh sách -->
        <DataGrid x:Name="dgProduction" Grid.Row="2"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" AlternatingRowBackground="#f8f9fa">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"          Binding="{Binding PlanId}"             Width="40"/>
                <DataGridTextColumn Header="Kế hoạch"    Binding="{Binding PlanName}"           Width="*"/>
                <DataGridTextColumn Header="Món ăn"      Binding="{Binding DishName}"           Width="*"/>
                <DataGridTextColumn Header="Số lượng"    Binding="{Binding PlannedQuantity}"    Width="70"/>
                <DataGridTextColumn Header="Ngày SX"     Binding="{Binding ProductionDate, StringFormat=dd/MM/yyyy}" Width="90"/>
                <DataGridTextColumn Header="Trạng thái"  Binding="{Binding Status}"             Width="90"/>
                <DataGridTextColumn Header="Ghi chú"     Binding="{Binding Note}"               Width="*"/>
                <DataGridTextColumn Header="Người tạo"   Binding="{Binding CreatedByNavigation.Username}" Width="90"/>
                <DataGridTemplateColumn Header="Thao tác" Width="190">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <Button Content="✅ Hoàn thành" Width="95" Height="24" Margin="2,0" Background="#28a745" Foreground="White" Tag="{Binding PlanId}" Click="BtnComplete_Click"/>
                                    <Button Content="❌ Từ chối" Width="70" Height="24" Margin="2,0" Background="#dc3545" Foreground="White" Tag="{Binding PlanId}" Click="BtnReject_Click"/>
                                </StackPanel>
                                <TextBlock Text="Đã hoàn thành" Foreground="#28a745" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="Đã từ chối" Foreground="#dc3545" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Rejected">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

   
</Window>

